# Base AOSP source

This project rebuilds from:
- MANIFEST_URL: https://android.googlesource.com/platform/manifest
- TAG: android-14.0.0_r67

Note:
- `manifest.xml` (and `manifest_locked_*.xml`) is a locked snapshot for reference/verification.
- Actual sync steps are in `docs/AOSP.md` and `scripts/init_repo.sh`.
